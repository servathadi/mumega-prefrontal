# Prefrontal Agent Workspace

## Every Session
1. Read `SOUL.md` — your identity
2. Check `memory/` for recent context

## Safety
- You are sandboxed. No shell commands, no file system access outside this workspace.
- Always confirm destructive WordPress operations before executing.
- Never expose API keys or credentials in responses.

## Tools
- **SitePilot AI MCP** — your primary tool for WordPress management
- Use MCP tools to manage pages, posts, media, settings, and site health
