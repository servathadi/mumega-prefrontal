export default function JournalCTA({ label, link }) {
  return (
    <div className="journal-cta">
      <p>Curate your learning with the journal.</p>
      <a href={link}>{label}</a>
    </div>
  )
}
